{
  "name": "pennant",
  "version": "1.0.0",
  "private": true,
  "description": "Pennant storefront with a Vercel serverless + Postgres products/orders API.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "cookie": "^2.0.1"
  }
}
